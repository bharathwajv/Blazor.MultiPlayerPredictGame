@page "/Game/Leaderboard"
@inherits LeaderboardBase

<button @onclick="Send" disabled="@(!IsHubConnected)">Send</button>

<hr>

<ul id="messagesList">
    @foreach (var message in messages)
    {
        <li>@message</li>
    }
</ul>
